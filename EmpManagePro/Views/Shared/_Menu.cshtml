<nav class="navbar navbar-expand-lg navbar-light">
    <a class="navbar-brand" href="@Url.Action("Index", "Home")">EmpManagePro</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Index", "Home")">Inicio</a>
            </li>
            @if (User.IsInRole("Admin"))
            {
                <li class="nav-item dropdown ml-auto">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Turnos
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="@Url.Action("Index", "Turnos")">Turnos</a></li>
                        <li><a class="dropdown-item" href="@Url.Action("Create", "Turnos")">Crear Turno</a></li>
                    </ul>
                </li>
            }
            else if (User.IsInRole("Empleado"))
            {
                <li class="nav-item dropdown ml-auto">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Turnos
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="@Url.Action("Index", "Turnos")">Turnos</a></li>
                    </ul>
                </li>
            }
            @if (User.IsInRole("Admin"))
            {
                <li class="nav-item dropdown ml-auto">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Nóminas
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="@Url.Action("GestionNominas", "Nominas")">Gestión de Nóminas</a></li>
                        <li><a class="dropdown-item" href="@Url.Action("PagosTotales", "Nominas")">Pagos Totales</a></li>
                    </ul>
                </li>
            }
            else if (User.IsInRole("Empleado"))
            {
                <li class="nav-item dropdown ml-auto">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Nóminas
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="@Url.Action("HistoricoPagos", "Nominas")">Historial de Pagos</a></li>
                    </ul>
                </li>
            }
            @if (User.IsInRole("Admin"))
            {
                <li class="nav-item dropdown ml-auto">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Evaluaciones
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="@Url.Action("Index", "Evaluaciones")">Ver Evaluaciones</a></li>
                        <li><a class="dropdown-item" href="@Url.Action("Create", "Evaluaciones")">Crear Evaluación</a></li>
                    </ul>
                </li>
            }
            else if (User.IsInRole("Empleado"))
            {
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("MisEvaluaciones", "Evaluaciones")">Mis Evaluaciones</a>
                </li>
            }
           @*  <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Index", "Notificaciones")">Notificaciones</a>
            </li> *@


            @if (User.IsInRole("Admin"))
            {
                <li class="nav-item dropdown ml-auto">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Perfil
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="@Url.Action("Profile", "Empleados", new { isSelf = true })">Mi perfil</a></li>
                        <li><a class="dropdown-item" href="@Url.Action("Create", "Empleados")">Registro de Empleados</a></li>
                        <li><a class="dropdown-item" href="@Url.Action("Index", "Empleados")">Gestión de Empleados</a></li>
                        <li><a class="dropdown-item" href="@Url.Action("Logout", "Account")">Cerrar sesión</a></li>
                    </ul>
                </li>
            }
            else if (User.IsInRole("Empleado"))
            {
                <li class="nav-item dropdown ml-auto">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Perfil
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="@Url.Action("Profile", "Empleados")">Mi perfil</a></li>
                        <li><a class="dropdown-item" href="@Url.Action("Logout", "Account")">Cerrar sesión</a></li>
                    </ul>
                </li>
            }

        </ul>
    </div>
</nav>
